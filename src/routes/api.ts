import { Hono } from 'hono';

// Public API router.
// This template keeps it intentionally empty so developers can add
// custom endpoints without touching internal trigger/menu wiring.
export const api = new Hono();
